name: inception

services:
  mariadb:
    build: ./requirements/mariadb
    env_file: .env
    networks:
      - inception
    volumes:
      - mariadb_data:/var/lib/mysql
    restart: on-failure

volumes:
  mariadb_data:

networks:
  inception:
    driver: bridge